# 使用者操作偏好

最後更新：2026-04-27 Asia/Taipei

## Live 下單默認
- 先讀：`[[我的箴言語錄]]`
- 除非使用者明確說明要「模擬」、「測試」、「dry-run」或其他同義表示，否則所有下單、排程、委託與送單要求一律視為真實下單。
- 不能自行把下單需求降級成模擬；若系統預設、舊筆記或 guardrail 與這個偏好衝突，必須先指出衝突，再由使用者決定。
- 即使預設視為真實下單，仍保留既有 guardrail：交易日檢查、duplicate-order 檢查、券商登入、CA 驗證與顯式 live 送單流程。

## 系統現況說明
- 目前程式層仍保留部分 `default_simulation` / `--live` / `--confirm-live` 的既有控制，這是執行機制，不等於使用者意圖。
- 解析需求時應先以使用者偏好判斷「要不要真實下單」，再檢查系統 guardrail 是否允許執行。

## 目前明確授權 / 偏好
- 2026-W18 永豐自動交易已開 weekly execution，週預算 `60000 TWD`。
- 永豐整包買進只使用 上游預選；10:00 後才會執行永豐自動交易的確定 上游預選工作。
- 目前唯一受 guard 的單股 live automation 內容是 `2330 / Buy / IntradayOdd / 1股 / 09:10 / price cap 2100`；若排程不存在，不能憑這行自行新增，仍需使用者明確要求。
- 整包 live 買進修復後不直接續下；必須先寄 repair_confirmation 報告，依使用者 mail 回覆內容判斷。

## 備註
- 這份筆記是長期偏好記憶，不能放在會被 `sync_obsidian` 覆寫的自動產生檔案中。
